# ExplainMyBody Backend Requirements
# FastAPI 기반 백엔드 서버

# ============================================================
# Core Framework
# ============================================================
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
python-dotenv>=1.0,<2.0

# ============================================================
# Database
# ============================================================
sqlalchemy>=2.0,<3.0
psycopg2-binary>=2.9,<3.0
alembic>=1.13,<2.0  # 데이터베이스 마이그레이션 (선택적)

# ============================================================
# Data Validation
# ============================================================
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
email-validator>=2.1,<3.0

# ============================================================
# OCR (PaddleOCR)  # 추후에 OCR 기능 업데이트 후 수정필요 #fixme
# ============================================================
paddlepaddle==3.2.2
paddleocr==3.3.2
opencv-python>=4.9,<5.0
pillow>=10.0,<13.0
pypdfium2>=4.30,<6.0

# ============================================================
# Body Type Analysis
# ============================================================
numpy>=1.26,<3.0

# ============================================================
# LLM Integration (추후 팀원이 추가할 수 있음)
# ============================================================
# openai>=1.0,<2.0  # OpenAI API
# anthropic>=0.18,<1.0  # Anthropic Claude API
# langchain>=0.1,<1.0  # LangChain (선택적)

# ============================================================
# Utilities
# ============================================================
httpx>=0.27,<1.0
python-multipart>=0.0.9  # 파일 업로드 지원

# ============================================================
# Development (개발 시에만 사용) # 추후에 수정필요 #fixme
# ============================================================
# pytest: 로직이 정상 작동하는지 확인하는 기본 테스트 도구
pytest>=8.0,<9.0
# pytest-asyncio: FastAPI의 비동기(async) 함수 테스트를 위해 필수
pytest-asyncio>=0.23,<1.0
