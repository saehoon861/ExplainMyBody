# 한국어 논문 자동 수집 가이드

ExplainMyBody RAG 코퍼스를 위한 한국어 논문 자동/반자동 수집 전체 워크플로우

## 📊 목표

| 도메인 | 영어 (PubMed) | 한국어 | 총합 |
|--------|---------------|--------|------|
| 단백질/근육 | 800개 | - | 800개 |
| 체지방 감량 | 800개 | - | 800개 |
| 한국 식단 | 300개 | **300개** | 600개 |
| 체형/인바디 | 500개 | **300개** | 800개 |
| **총합** | **2400개** | **600개** | **3000개** |

## 🚀 빠른 시작 (3시간 워크플로우)

### Step 1: 의존성 설치 (5분)

```bash
cd /home/user/projects/ExplainMyBody/llm/ragdb_collect

pip install -r requirements.txt
```

### Step 2: Google Scholar 자동 수집 (1-2시간)

**가장 효율적인 방법**으로 200-300개의 한국어 논문을 자동으로 수집합니다.

```bash
python google_scholar_korean_collector.py
```

**예상 결과**:
- `outputs/google_scholar_korean_XXXXXX.json`: 전체 논문
- `outputs/korean_diet_scholar_XXXXXX.json`: 한국 식단 (150개)
- `outputs/body_composition_scholar_XXXXXX.json`: 체형 분석 (150개)

**소요 시간**: 약 1-2시간 (rate limiting 때문)

**팁**:
- 네트워크가 불안정하면 중간에 멈출 수 있음 → 재실행하면 이어서 수집
- 프록시 사용 옵션: `use_proxy=True` (코드 수정)

### Step 3: 정부 보고서 수집 (30-60분)

공식 문서에서 100-150개의 고품질 한국어 초록을 수집합니다.

#### 3-1. 보고서 다운로드

**추천 사이트**:

1. **보건복지부** (https://www.mohw.go.kr/)
   - 검색: "국민건강영양조사", "영양섭취기준"
   - 예시: `2023 국민건강영양조사 결과보고서.pdf`

2. **질병관리청** (https://www.kdca.go.kr/)
   - 검색: "KNHANES", "만성질환 예방"
   - 예시: `만성질환 예방을 위한 식생활 가이드.pdf`

3. **식약처** (https://www.mfds.go.kr/)
   - 검색: "기능성 식품", "영양성분 기준"
   - 예시: `기능성 표시 가이드라인.pdf`

4. **대한영양학회** (http://www.kns.or.kr/)
   - 검색: "단백질 섭취", "한국인 영양소 섭취기준"
   - 예시: `2020 한국인 영양소 섭취기준.pdf`

5. **체육과학연구원** (https://www.sports.re.kr/)
   - 검색: "운동 영양", "체력 평가"
   - 예시: `운동선수 영양 가이드.pdf`

#### 3-2. 템플릿 파일 수정

```bash
# 템플릿 생성
python government_report_parser.py
```

이 명령어를 실행하면 `outputs/government_reports_template.json`이 생성됩니다.

템플릿 예시:
```json
[
  {
    "pdf_path": "downloads/mohw_nutrition_2023.pdf",
    "title": "2023 국민건강영양조사 영양 섭취 기준",
    "domain": "korean_diet",
    "year": 2023,
    "authors": ["보건복지부", "질병관리청"],
    "source": "보건복지부"
  }
]
```

#### 3-3. PDF 일괄 처리

```bash
python government_report_parser.py --process
```

**예상 결과**:
- `outputs/government_reports_XXXXXX.json`: 80-130개 문서

### Step 4: 학술지 논문 수집 (30-60분)

학회 사이트에서 CSV/Excel로 논문 목록을 다운로드하고 파싱합니다.

#### 4-1. 학술지 사이트에서 CSV 다운로드

**추천 학술지**:

1. **대한영양학회** (http://www.kns.or.kr/)
   - 무료 논문 多, CSV 다운로드 가능
   - 검색: "한국 식단", "단백질 섭취"

2. **한국체육학회** (http://www.koreasportscience.org/)
   - 운동영양학회지
   - 검색: "근육 증가", "체성분"

3. **한국식품영양과학회** (http://www.kosfost.or.kr/)
   - 발효식품, 한식 연구
   - 검색: "김치", "된장", "발효"

4. **대한비만학회** (http://www.kosso.or.kr/)
   - 체지방, 체성분 연구
   - 검색: "체지방률", "비만 관리"

5. **대한노인병학회** (http://www.geriatrics.or.kr/)
   - 근감소증, 노화 연구
   - 검색: "근감소증", "노인 영양"

#### 4-2. CSV 파일 처리

```bash
# 템플릿 생성
python society_csv_parser.py

# CSV 파일 처리
python society_csv_parser.py --process [CSV파일경로]
```

**인터랙티브 입력**:
- 도메인 선택: 1-4 (기본: 3)
- 출처 입력: 학회명 (예: 대한영양학회)

**예상 결과**:
- `outputs/society_papers_XXXXXX.json`: 130-230개 논문

### Step 5: 전체 병합 (5분)

모든 소스를 하나로 병합하고 중복을 제거합니다.

```bash
python merge_korean_corpus.py
```

**자동 처리 내용**:
1. PubMed 영어 논문 로드 (2400개)
2. Google Scholar 한국어 논문 로드 (200-300개)
3. 정부 보고서 로드 (80-130개)
4. 학술지 논문 로드 (130-230개)
5. 중복 제거 (제목 유사도 85% 이상)
6. 최종 코퍼스 저장

**예상 결과**:
- `outputs/ragdb_final_corpus_XXXXXX.json`: **2800-3060개** 전체 논문
- `outputs/ragdb_final_stats_XXXXXX.json`: 통계
- `outputs/protein_hypertrophy_final_XXXXXX.json`: 도메인별 분할 파일
- `outputs/fat_loss_final_XXXXXX.json`
- `outputs/korean_diet_final_XXXXXX.json`
- `outputs/body_composition_final_XXXXXX.json`

## 📂 파일 구조

```
ragdb_collect/
├── main.py                              # PubMed 영어 논문 수집
├── google_scholar_korean_collector.py    # Google Scholar 한국어 논문 자동 수집
├── government_report_parser.py           # 정부 보고서 PDF 파싱
├── society_csv_parser.py                 # 학술지 CSV/Excel 파싱
├── merge_korean_corpus.py                # 전체 병합 및 중복 제거
├── models.py                             # 데이터 모델
├── config.py                             # 검색어 설정
├── pubmed_collector.py                   # PubMed API 클라이언트
├── kci_collector.py                      # KCI 수동 수집 가이드
├── requirements.txt                      # 의존성
└── outputs/                              # 수집 결과
    ├── ragdb_corpus_XXXXXX.json          # PubMed 영어 논문
    ├── google_scholar_korean_XXXXXX.json # Google Scholar 한국어
    ├── government_reports_XXXXXX.json    # 정부 보고서
    ├── society_papers_XXXXXX.json        # 학술지 논문
    └── ragdb_final_corpus_XXXXXX.json    # 최종 병합 결과
```

## 🔍 각 스크립트 상세 가이드

### 1. google_scholar_korean_collector.py

**기능**: Google Scholar에서 한국어 논문을 자동으로 검색하고 수집

**장점**:
- ✅ 완전 자동화
- ✅ 초록 품질 양호
- ✅ 다양한 학술지 커버

**단점**:
- ⚠️ Rate limiting (느림)
- ⚠️ 초록 없는 논문도 있음

**사용법**:
```python
# 기본 실행
python google_scholar_korean_collector.py

# 코드 내 설정 변경 가능
collector = GoogleScholarKoreanCollector(
    use_proxy=False,      # 프록시 사용 여부
    rate_limit=3.0        # 요청 간 대기 시간 (초)
)
```

**커스터마이징**:
```python
# 검색어 추가/수정 (코드 내)
KOREAN_DIET_QUERIES = [
    "한식 식사패턴 건강",
    "김치 섭취 효과",
    # 여기에 추가...
]

# 목표 개수 조정
korean_diet_papers = collector.collect_domain(
    domain='korean_diet',
    queries=KOREAN_DIET_QUERIES,
    target_count=300,     # 300개로 늘리기
    year_from=2010        # 2010년 이후만
)
```

### 2. government_report_parser.py

**기능**: PDF 보고서에서 초록/요약 섹션을 자동으로 추출

**장점**:
- ✅ 공식 문서 (신뢰도 높음)
- ✅ 한국 특화 데이터

**단점**:
- ⚠️ PDF 다운로드 필요 (수동)
- ⚠️ 일부 PDF는 추출 실패 가능

**워크플로우**:
```bash
# 1. 템플릿 생성
python government_report_parser.py

# 2. outputs/government_reports_template.json 수정
# 3. PDF 다운로드 및 경로 입력

# 4. 일괄 처리
python government_report_parser.py --process
```

**템플릿 형식**:
```json
[
  {
    "pdf_path": "downloads/report.pdf",      # PDF 파일 경로
    "title": "보고서 제목",                    # 제목
    "domain": "korean_diet",                 # 도메인 선택
    "year": 2023,                            # 발행 연도
    "authors": ["기관명"],                    # 저자/기관
    "source": "보건복지부"                    # 출처
  }
]
```

**추출 로직**:
- 키워드 검색: "요약", "초록", "Summary", "Abstract" 등
- 최대 50줄 또는 종료 키워드까지 추출
- 최소 100자 이상만 유효

### 3. society_csv_parser.py

**기능**: 학술지 CSV/Excel 파일을 파싱하여 PaperMetadata로 변환

**장점**:
- ✅ 대량 수집 가능
- ✅ 초록 품질 보장
- ✅ 메타데이터 완전

**단점**:
- ⚠️ CSV 다운로드 필요 (수동)
- ⚠️ 일부 학회는 로그인 필요

**워크플로우**:
```bash
# 1. 템플릿 생성
python society_csv_parser.py

# 2. 학회 사이트에서 CSV 다운로드

# 3. CSV 처리
python society_csv_parser.py --process downloads/papers.csv

# 도메인 선택 (1-4)
# 출처 입력 (예: 대한영양학회)
```

**컬럼 자동 인식**:
- 제목: "제목", "title", "논문명", etc.
- 초록: "초록", "abstract", "요약", etc.
- 키워드: "키워드", "keywords", etc.
- 연도: "발행년도", "year", etc.
- 저자: "저자", "author", etc.

**Excel 지원**:
```bash
python society_csv_parser.py --process downloads/papers.xlsx
```

### 4. merge_korean_corpus.py

**기능**: 모든 소스를 병합하고 중복 제거

**중복 제거 로직**:
1. PMID가 같으면 중복
2. DOI가 같으면 중복
3. 제목 유사도 85% 이상이면 중복

**자동 처리**:
- outputs 폴더에서 최신 파일 자동 로드
- 도메인별, 언어별, 소스별 통계 생성
- 도메인별 분할 파일 생성

**출력 예시**:
```
📊 최종 결과:
  입력: 3200개
  중복 제거: 250개
  출력: 2950개

🎯 목표 대비 달성률:
  ✅ protein_hypertrophy: 800/800 (100.0%)
  ✅ fat_loss: 800/800 (100.0%)
  📈 korean_diet: 550/600 (91.7%)
  ✅ body_composition: 800/800 (100.0%)

  총합: 2950/3000 (98.3%)
```

## 🎯 추가 수집 방법 (선택)

### 방법 1: RISS 수동 수집

**RISS** (http://www.riss.kr/)는 한국 학위논문 및 학술지를 통합 검색할 수 있습니다.

1. RISS 접속
2. 검색어 입력 (예: "한식 영양")
3. "초록 있음" 필터 적용
4. Excel 다운로드
5. `society_csv_parser.py`로 처리

**예상 수집량**: 50-100개

### 방법 2: KCI 수동 수집

**KCI** (https://www.kci.go.kr/)는 한국학술지인용색인입니다.

1. KCI 접속
2. 검색어 입력
3. "초록 제공" 논문만 선택
4. CSV 다운로드
5. `society_csv_parser.py`로 처리

**예상 수집량**: 100-200개

### 방법 3: DBpia 무료 논문

**DBpia** (https://www.dbpia.co.kr/)의 무료 공개 논문

1. DBpia 접속
2. "무료 논문" 필터
3. 검색 및 다운로드
4. 수동으로 JSON 작성 또는 CSV 변환

**예상 수집량**: 50-100개

## 📊 예상 수집량 총정리

| 소스 | 자동화 수준 | 예상 개수 | 소요 시간 |
|------|------------|-----------|----------|
| Google Scholar | 🟢 완전 자동 | 200-300개 | 1-2시간 |
| 정부 보고서 | 🟡 반자동 | 80-130개 | 30-60분 |
| 학술지 CSV | 🟡 반자동 | 130-230개 | 30-60분 |
| RISS (선택) | 🔴 수동 | 50-100개 | 30분 |
| KCI (선택) | 🔴 수동 | 100-200개 | 30분 |
| **총합** | - | **560-960개** | **3-5시간** |

## 🚨 주의사항

### Rate Limiting

**Google Scholar**:
- 너무 빠르게 요청하면 IP 차단 가능
- 기본 설정: 3초 대기 (안전)
- 프록시 사용 시 더 빠름 (선택)

**해결책**:
```python
# google_scholar_korean_collector.py 수정
collector = GoogleScholarKoreanCollector(
    use_proxy=True,      # 프록시 사용
    rate_limit=2.0       # 2초로 줄이기 (주의!)
)
```

### PDF 추출 실패

일부 PDF는 스캔본이거나 암호화되어 텍스트 추출이 안 될 수 있습니다.

**해결책**:
1. OCR 도구 사용 (예: Tesseract)
2. 수동으로 초록 복사 → JSON에 입력

### CSV 인코딩 문제

한국어 CSV는 `cp949` 또는 `euc-kr` 인코딩일 수 있습니다.

**자동 처리**:
```python
# society_csv_parser.py가 자동으로 시도
# utf-8 실패 → cp949 시도
```

**수동 변환** (필요시):
```bash
iconv -f cp949 -t utf-8 input.csv > output.csv
```

### 저작권

- ✅ 초록(Abstract)은 일반적으로 공개 사용 가능
- ✅ 공공기관 보고서는 공공누리 적용 (제한 없음)
- ⚠️ Full-text는 저작권 확인 필요
- ⚠️ 상업적 사용 시 라이선스 확인

## 🔄 다음 단계

수집 완료 후:

### 1. Chunking

```bash
cd /home/user/projects/ExplainMyBody/llm

# TODO: chunking 파이프라인 작성 예정
python chunk_abstracts.py \
  --input ragdb_collect/outputs/ragdb_final_corpus_XXXXXX.json \
  --output ragdb_chunks.json
```

### 2. Embedding 생성

```bash
# TODO: embedding 파이프라인 작성 예정
python create_embeddings.py \
  --input ragdb_chunks.json \
  --model text-embedding-3-small \
  --output ragdb_embeddings.json
```

### 3. DB 저장

```bash
# TODO: pgvector 저장 파이프라인 작성 예정
python save_to_db.py \
  --input ragdb_embeddings.json
```

## 📚 참고 자료

### API/도구
- Google Scholar (scholarly): https://scholarly.readthedocs.io/
- pdfplumber: https://github.com/jsvine/pdfplumber
- pandas: https://pandas.pydata.org/

### 한국 데이터 소스
- KCI: https://www.kci.go.kr/
- RISS: http://www.riss.kr/
- KoreaScience: https://www.koreascience.or.kr/
- DBpia: https://www.dbpia.co.kr/

### 정부/공공기관
- 보건복지부: https://www.mohw.go.kr/
- 질병관리청: https://www.kdca.go.kr/
- 식약처: https://www.mfds.go.kr/

### 학회
- 대한영양학회: http://www.kns.or.kr/
- 한국체육학회: http://www.koreasportscience.org/
- 한국식품영양과학회: http://www.kosfost.or.kr/

## 🤝 트러블슈팅

### "scholarly 설치 실패"

```bash
pip install --upgrade pip
pip install scholarly
```

### "PDF 읽기 실패"

```bash
pip install pdfplumber
```

일부 PDF는 보호되어 있을 수 있습니다. 가능하면 원본 사이트에서 텍스트 버전을 다운로드하세요.

### "Google Scholar 차단"

IP가 차단되었을 수 있습니다. 다음을 시도하세요:

1. 1시간 대기 후 재시도
2. 프록시 사용 옵션 활성화
3. VPN 사용

### "초록이 너무 짧음"

`models.py`에서 최소 길이 조정:

```python
# 기본값: 100자
if len(abstract) < 100:  # 50자로 줄이기
    continue
```

## 📝 라이선스

MIT License
